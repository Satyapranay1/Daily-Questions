class Solution {
    public int findNumbers(int[] nums) {
        int ans = 0;
        for (int i = 0; i < nums.length; i++){
            if (((int)(Math.log(nums[i]) / Math.log(10)) % 2 == 0)){
                ans++;
            }
        }
        return ans;
    }
}
